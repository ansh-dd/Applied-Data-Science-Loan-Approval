# Benchmark of Existing Solutions

Approaches: Rule-based assessment, Logistic Regression, Decision Tree, Random Forest, Gradient Boosting/XGBoost.

Improvements: use both classes, prevent target leakage, align schemas, compare multiple models, report multiple metrics, use SHAP/LIME, and use Git/DVC/MLflow for reproducibility.
